import 'package:flutter/material.dart';
import 'package:scale_kuwait_mobile_app/screens/onboarding_slider/components/body.dart';

class OnboardingSliderScreen extends StatelessWidget {
  const OnboardingSliderScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
